// Para comentar mis lineas de código se escribe //
// alert('Hola desde mi archivo JS');

// La declaración de una variable
var x = 1;
var miVariable = 122 //number

// Los tipos NUMBER tiene todas las operadores
var suma = x + miVariable;
var resta = x - miVariable;
var multi = x * miVariable;
var div = x / miVariable;
alert(div);

// STRINGS
// El texto tiene que estar encerrado entre comillas dobles " " o simples ' '
var miTexto = 'JavaScript es chido ';
var miLetra = 'J';
var miNumero = '304'
alert(miTexto);

//camelCase: JavaScript suele manejar camelCase como "standard"
//snake_case

//CONCATENACION: Pega los strings
//String + String = CONCATENA
//Number + Number = SUMA
//String + Number = CONCATENA (Se trata Number como String)

alert(miTexto + miLetra);


//BOOLEAN
var verdadero = true;
var falso = false;